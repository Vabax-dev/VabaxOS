# Kokoro module for Speech Dispatcher, VabaxOS (ADR-0019).
# The voice of each language. At boot vabaxos-voice-select writes the
# fastest voice for this computer to /var/lib/vabaxos/voice.conf, which
# takes precedence over these lines.
DefaultVoice it im_nicola
DefaultVoice en af_heart
