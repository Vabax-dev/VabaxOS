# Chromium and Electron programs expose their accessibility tree only when
# asked: with this, Orca can read them from the start (GNOME 50 known issue,
# discourse.gnome.org/t/38619). Firefox and Thunderbird follow GNOME's
# accessibility setting by themselves.
ACCESSIBILITY_ENABLED=1
