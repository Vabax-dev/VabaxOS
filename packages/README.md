# packages/

Pacchetti Debian di VabaxOS, uno per cartella, con prefisso `vabaxos-` ([ADR-0008](../docs/decisions/0008-pacchetti-e-applicazioni.md)).

Previsti per la v0.1: `vabaxos-settings`, `vabaxos-accessibility`, `vabaxos-branding`.
