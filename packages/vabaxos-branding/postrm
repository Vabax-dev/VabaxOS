#!/bin/sh
set -e
if [ "$1" = remove ] || [ "$1" = purge ]; then
    rm -f /usr/lib/os-release
    dpkg-divert --package vabaxos-branding --remove --rename \
        --divert /usr/lib/os-release.debian /usr/lib/os-release
fi
