# VabaxOS live system: console speech goes through the live user's PipeWire
# (UID 1000). Start the user manager first, so espeakup finds its socket at
# once, also in recovery mode where there is no welcome to wait for.
[Unit]
Wants=user@1000.service
After=user@1000.service
