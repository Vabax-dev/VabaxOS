# VabaxOS live system: console speech goes through the live user's PipeWire,
# which runs from boot (linger): start after it, so espeakup finds its
# socket at once, also in recovery mode where there is no welcome to wait for.
[Unit]
After=user@1000.service
