# VabaxOS live system: the welcome speaks through the live user's PipeWire
# (UID 1000), the one sound server that console speech and Orca use too.
# Start the user manager first; it waits for live-config to create the user.
[Unit]
Wants=user@1000.service
After=user@1000.service
