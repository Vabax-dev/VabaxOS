# VabaxOS live system: the live user ("user", UID 1000, live-config) lingers,
# so its PipeWire runs from boot (/var/lib/systemd/linger/user). The welcome,
# console speech and Orca then all speak through that one sound server and
# never fight for the sound card.
[Unit]
After=user@1000.service
