# VabaxOS live system: the welcome speaks through the live user's PipeWire
# (UID 1000), the one sound server that console speech and Orca use too.
# Start the user manager first (vabaxos-live-audio.service, live only).
[Unit]
Wants=vabaxos-live-audio.service
After=vabaxos-live-audio.service user@1000.service
